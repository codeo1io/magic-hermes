"""Python subc client for magic-hermes.

Wire-compatible with @cortexkit/subc-client and the Rust subc consumers:
loopback TCP transport, HMAC-SHA256 framing. Protocol reference:
upstream packages' subc-client README (byte-for-byte compat with
subc-transport).

Implementation pending planning — this stub records the contract only.
"""
